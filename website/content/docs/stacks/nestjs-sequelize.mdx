---
title: NestJS + Sequelize setup
description: Wire fail-closed multi-tenancy into a NestJS 11 + Sequelize app - a copy-pasteable integration-agent prompt, plus the manual walkthrough.
---

`tenancyjs-cli init` doesn't scaffold NestJS yet, so this stack is a manual wire-up - a few files, for a
**new or existing** NestJS + Sequelize app. This page gives you two ways to do it: hand the
**integration-agent prompt** to your AI assistant, or follow the **manual walkthrough**. The agent
assesses your app first, integrates with your existing models and auth, and finishes with an isolated
leak test - because with fail-closed isolation you verify, you don't trust.

This is **row-level** on PostgreSQL. The Sequelize adapter is a _callback facade_ backed by **forced
Postgres RLS** - the facade rejects unsafe criteria and the database policy is a hard backstop, so a
query can't escape its tenant scope. The one rule that matters most: only ever call the protected
`client.model(...)` facade inside `tenancy.run(...)` - never the native Sequelize model.

<Callout type="info">
  Running **MySQL**? This page is PostgreSQL (row-level + forced RLS). MySQL has no schema-per-tenant
  and its row-level mode is experimental and facade-only - use
  [database-per-tenant on MySQL](/docs/stacks/mysql-database-per-tenant) instead.
</Callout>

<Callout type="info">
  TenancyJS ships **dual ESM + CommonJS** (0.1.0+), so a default CommonJS NestJS project works with no
  conversion — `require` or import it as usual.
</Callout>

<Callout type="info">
  This page uses **row-level** (the simplest strategy). The same stack also supports
  [schema-per-tenant](/docs/stacks/schema-per-tenant) and
  [database-per-tenant](/docs/stacks/database-per-tenant) — switch the adapter’s `strategy` and add the
  [provisioning recipe](/docs/guides/provisioning) for your ORM.
</Callout>

## Integration-agent prompt

Paste this into an assistant that can edit your repo and run commands (Claude Code, Cursor, …). It
assesses your app first, integrates with what already exists, verifies each step, and finishes with an
isolated leak test. It's pinned to the real API, so it won't invent factory names.

````text
# TenancyJS Integration Agent - NestJS 11 + Sequelize (row-level, PostgreSQL)

You are an integration agent. Wire fail-closed multi-tenancy into THIS project with TenancyJS,
integrating with whatever already exists — do NOT assume a greenfield app. Execute steps in order;
after each, verify it applied before continuing. If a step does not apply to this repo, say so and
adapt. Do NOT invent or assume API names, versions, or enum values, and never assert them from memory (your training data may be stale — library versions change). Verify against the real thing FIRST, then claim: `npm view <pkg> version` for a version, and the installed package's own types (in node_modules) or the linked docs for exact signatures and literal option values (e.g. the strategy is the string `"rowLevel"`).

Source of truth (fetch if a detail is missing, do not guess):
- https://tenancyjs.pages.dev/docs/adapters/sequelize
- https://tenancyjs.pages.dev/docs/integrations/nestjs
- https://tenancyjs.pages.dev/docs/concepts/limitations
- https://tenancyjs.pages.dev/docs/guides/testing-isolation
- https://tenancyjs.pages.dev/docs/guides/resolving-tenants

## Step 0 - Assess the app (change nothing yet)
- Report the Node version (abort if < 24), the `@nestjs/core` (v11) and `sequelize` versions, and
  confirm the database is PostgreSQL - this stack relies on forced RLS, which MySQL does not have. If
  this is not a NestJS + Sequelize + Postgres app, stop.
- Inventory the Sequelize models: list every model and mark each as tenant-owned or global/central.
- Find how the current user is determined per request (NestJS guard, `req.user`) — the `authorize` hook
  will need it.
- Detect any EXISTING tenant concept (a `tenantId`/`orgId` attribute, a workspaces/orgs table). If one
  exists, REUSE it — do not add a parallel one.
- Summarize what you found + the integration plan, and confirm the ISOLATION STRATEGY with the user:
  row-level (this page) vs schema-per-tenant vs database-per-tenant. If row-level does not fit, point
  them to https://tenancyjs.pages.dev/docs/strategies/choosing and stop.

## Step 1 - Install
Run:
```sh
npm install tenancyjs-core tenancyjs-adapter-sequelize tenancyjs-integration-nest tenancyjs-identifiers sequelize pg
```
Verify all six appear in package.json dependencies.

## Step 2 - Models: classify what exists, add only what's missing
- Ensure a `Tenant` model exists (`id` string PK, `status` string default "active"). If the app already
  has a tenants/workspaces/orgs table, use THAT — do not add a parallel one.
- For every EXISTING tenant-owned model, add a non-null `tenantId` attribute mapped to a `tenant_id`
  column, and an index on it. Only create a new example model (e.g. `Post`) if the app has no domain
  models yet.
- If you add `tenantId` to a model backed by a table that already has rows, backfill it before making it
  non-null (see Step 10 - Roll out).
Verify each tenant model declares both the attribute name and the physical column name.

## Step 3 - Forced-RLS migration (the database backstop)
Row-level isolation here is enforced by the database, not just the facade. Write a migration, run by an
OWNER role, that for every tenant table:
- Creates the table with a `tenant_id` column.
- Runs `ALTER TABLE posts ENABLE ROW LEVEL SECURITY` and `ALTER TABLE posts FORCE ROW LEVEL SECURITY`.
- Creates a policy `posts_tenant_isolation` USING/WITH CHECK that the row's `tenant_id` equals
  `current_setting('tenancyjs.tenant_id', true)` OR `current_setting('tenancyjs.is_central', true)` is
  true.
The application must connect as a RUNTIME role that is NOT the table owner, NOT `BYPASSRLS`, and NOT a
superuser - otherwise FORCE RLS is skipped and the backstop is silently off. Verify the runtime role has
none of those attributes.

## Step 4 - tenancy.ts (the single source of truth)
Create `src/tenancy.ts`. It must:
- Construct ONE `TenancyManager<Tenant>` and export it.
- Construct the `createSequelizeTenancy({ manager, sequelize, tenantModels: [...] })` adapter. Each entry
  is `{ model, table, tenantAttribute: "tenantId", tenantColumn: "tenant_id" }`. Export it as `tenancy`.
- Build a `TenantResolutionChain` with `new HeaderTenantResolver()` and a store whose `find(identifier)`
  looks the tenant up and returns `[{ tenant: { id }, status }]` (or `[]`). Include an
  `authorize({ tenant, principal }) => boolean` membership check that fails closed (a resolved tenant is
  not an authorized one); the chain refuses to construct without it.
- Export `manager`, `resolver`, and `tenancy`.

## Step 5 - Register TenancyModule
In `app.module.ts`, add `TenancyModule.forRoot({ manager, resolver })` to imports (import both from
`./tenancy`). Also pass `principal: (req) => req.user` so the guard hands the authenticated user to the
chain's `authorize` check (register it after your auth). Do not register a second guard/interceptor. Do
NOT pass `tenancy` as `executor` - a callback facade is called directly in the handler, not injected as
an executor.

## Step 6 - Scope the routes
For every controller handler that touches tenant data:
- Add `@TenantRoute()` (from `tenancyjs-integration-nest`).
- Read/write ONLY through `tenancy.run((client) => client.model(Post).findAll(...))`. Never touch the
  native Sequelize model, `.query()`, includes, or `Op.*` operators - the facade rejects them. REMOVE any
  manual `where: { tenantId }` filters; the scope injects the predicate. Leave public routes unmarked.

## Step 7 - Central (cross-tenant) work
For admin/cross-tenant handlers, do NOT leave them unscoped. Wrap the work in
`manager.runInCentralContext(() => tenancy.run(...))` explicitly. Unscoped tenant access must throw.

## Step 8 - Validate the contract at boot
Call `await tenancy.validate()` during bootstrap (before serving traffic). It confirms every tenant
table has RLS enabled + forced and the isolation policy is present; it throws if the backstop is missing.
Call `tenancy.close()` on shutdown.

## Step 9 - Verify isolation (an isolated test - it does NOT touch app data)
Create a two-tenant test (Vitest or Jest) that runs against a TEST database or a rolled-back transaction
- never seed tenants into real app data. It creates tenant A and tenant B, writes a row under EACH with
the SAME primary key, and asserts querying as A never returns B's row. Then, connecting as the RUNTIME
role with NO `tenancyjs.tenant_id` GUC set, run a raw `SELECT` and assert it returns 0 rows - proving the
RLS backstop, not just the facade. Follow https://tenancyjs.pages.dev/docs/guides/testing-isolation.

## Step 10 - Roll out + verify
- `npx nest build` (or `npm run build`) - report errors.
- Boot the app (`npm run start`); confirm `tenancy.validate()` passes and it serves.
- Run the two-tenant test and report the result.
- Existing app: backfill `tenant_id` on any table that already had rows (set it for every row, then
  enforce non-null), then roll out incrementally - scope routes behind `@TenantRoute()`, keep
  admin/cross-tenant paths in `runInCentralContext`, and treat any thrown unscoped-access error as a
  route you still need to scope.

Report a summary of every file created/changed and any issue found.
````

<Callout type="warn">
  AI wiring is a starting point, not proof. The only thing that demonstrates isolation is the two-tenant
  colliding-id test in Step 9 - including the raw-query-as-runtime-role check - run it against a test
  database before you trust the result.
</Callout>

## Manual walkthrough

Prefer to wire it yourself? The same steps, with the code.

<Steps>

<Step>
### Install

```bash
npm install tenancyjs-core tenancyjs-adapter-sequelize tenancyjs-integration-nest tenancyjs-identifiers sequelize pg
```
</Step>

<Step>
### Force RLS in a migration

Row-level isolation is enforced by Postgres. Run this as the **owner** role; the app connects as a
separate **runtime** role that is not the owner, not `BYPASSRLS`, and not a superuser - otherwise
`FORCE` is skipped.

```sql
ALTER TABLE posts ENABLE ROW LEVEL SECURITY;
ALTER TABLE posts FORCE ROW LEVEL SECURITY;

CREATE POLICY posts_tenant_isolation ON posts
  USING (
    tenant_id = current_setting('tenancyjs.tenant_id', true)
    OR current_setting('tenancyjs.is_central', true) = 'true'
  )
  WITH CHECK (
    tenant_id = current_setting('tenancyjs.tenant_id', true)
    OR current_setting('tenancyjs.is_central', true) = 'true'
  );
```
</Step>

<Step>
### One `tenancy.ts`

The manager, the Sequelize adapter, and the resolver all live here - one source of truth.

```ts title="src/tenancy.ts"
import { TenancyManager } from "tenancyjs-core";
import { createSequelizeTenancy } from "tenancyjs-adapter-sequelize";
import { HeaderTenantResolver, TenantResolutionChain } from "tenancyjs-identifiers";
import { sequelize, Post, Tenant as TenantModel } from "./models";

export interface Tenant {
  readonly id: string;
}

export const manager = new TenancyManager<Tenant>();

// The protected facade. Only its client may touch tenant data.
export const tenancy = createSequelizeTenancy({
  manager,
  sequelize,
  tenantModels: [
    {
      model: Post,
      table: "posts",
      tenantAttribute: "tenantId",
      tenantColumn: "tenant_id",
    },
  ],
});

export const resolver = new TenantResolutionChain({
  resolvers: [new HeaderTenantResolver()], // X-Tenant-Id
  store: {
    async find(identifier) {
      const tenant = await TenantModel.findByPk(identifier.value);
      return tenant ? [{ tenant: { id: tenant.id }, status: tenant.status }] : [];
    },
  },
  // Required: does THIS authenticated user belong to the resolved tenant?
  authorize: ({ tenant, principal }) =>
    (principal as { teamIds: string[] }).teamIds.includes(tenant.id),
});
```
</Step>

<Step>
### Register the module

```ts title="src/app.module.ts"
import { Module } from "@nestjs/common";
import { TenancyModule } from "tenancyjs-integration-nest";
import { manager, resolver } from "./tenancy";

@Module({
  imports: [
    TenancyModule.forRoot({
      manager,
      resolver,
      principal: (req) => (req as { user?: unknown }).user, // the guard runs after auth
    }),
  ],
})
export class AppModule {}
```

Validate the RLS contract at boot, before serving:

```ts title="src/main.ts"
import { tenancy } from "./tenancy";
// ...after creating the Nest app, before app.listen():
await tenancy.validate();
```
</Step>

<Step>
### Scope your routes

Mark tenant routes; the interceptor opens the scope and the facade reads it. Call `tenancy.run(...)`
directly - never as an `executor`, and never the native model.

```ts title="src/posts.controller.ts"
import { Controller, Get } from "@nestjs/common";
import { TenantRoute } from "tenancyjs-integration-nest";
import { tenancy } from "./tenancy";
import { Post } from "./models";

@Controller("posts")
export class PostsController {
  @Get()
  @TenantRoute()
  list() {
    return tenancy.run((client) => client.model(Post).findAll({ status: "open" }));
  }
}
```
</Step>

<Step>
### Prove isolation

Write the [two-tenant colliding-id test](/docs/guides/testing-isolation): same primary key under A and
B, assert A never reads B's row, and - connecting as the runtime role with no `tenancyjs.tenant_id` GUC
set - assert a raw `SELECT` returns 0 rows. That second check proves the RLS backstop, not just the
facade.
</Step>

</Steps>

<Callout type="info">
  Cross-tenant admin work runs in an explicit central scope - `manager.runInCentralContext(() =>
  tenancy.run(...))`. There is no request-controlled central mode; a resolution failure fails closed,
  never central.
</Callout>

Full lifecycle and the security boundary: [NestJS integration](/docs/integrations/nestjs) ·
[Sequelize adapter](/docs/adapters/sequelize) · [Limitations](/docs/concepts/limitations).
