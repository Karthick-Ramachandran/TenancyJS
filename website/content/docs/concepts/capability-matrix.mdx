---
title: Capability matrix
description: Which databases, adapters, and strategies are tested-supported — and which aren't.
---

TenancyJS only marks a combination **supported** after a real two-tenant adversarial isolation test on a
real database: tenant A and tenant B with colliding ids, and a test that fails if A can ever read B's
row. This page is the honest source of truth for what has actually been proven.

## By database

Start here — it decides which strategies are even available to you.

| Database | Row-level | Schema-per-tenant | Database-per-tenant | Adapters |
| --- | :---: | :---: | :---: | --- |
| **PostgreSQL** | ✅ | ✅ | ✅ | Knex · Lucid · Prisma · TypeORM · Sequelize |
| **MySQL** | ✅ | — | — | Prisma |
| **MongoDB** | ✅ ¹ | — | — | Mongoose |

<Callout type="warn">
  **Schema-per-tenant and database-per-tenant are PostgreSQL-only.** They rely on Postgres mechanisms —
  forced row-level security and a transaction-local `search_path`. On **MySQL** and **MongoDB** the
  available strategy is **row-level** (via query-scoping, not RLS). If you're on MySQL or Mongo, that's
  your path — and it's a first-class one.
</Callout>

¹ **MongoDB is facade-only.** It has no row-level security, so isolation is enforced entirely by the
adapter's query facade — using the native model, collection, or connection bypasses it. Treat it as a
strong convention, not a database-level guarantee.

## By adapter × strategy

| Adapter | Database | Row-level | Schema-per-tenant | Database-per-tenant |
| --- | --- | :---: | :---: | :---: |
| **Knex** | PostgreSQL | ✅ | ✅ | ✅ |
| **Lucid** (AdonisJS) | PostgreSQL | ✅ | ✅ | ✅ |
| **Prisma** | PostgreSQL · MySQL | ✅ | — | ✅ |
| **TypeORM** | PostgreSQL | ✅ | — | — |
| **Sequelize** | PostgreSQL | ✅ | — | — |
| **Mongoose** | MongoDB | ✅ ¹ | — | — |

✅ tested-supported · — not available yet

## How each is enforced

- **PostgreSQL, row-level & schema-per-tenant** — forced Postgres **RLS** (the database rejects
  cross-tenant rows even under raw SQL), plus the adapter's facade. Schema-per-tenant adds a
  transaction-local `search_path` and an optional per-tenant role.
- **MySQL, row-level** — the Prisma adapter's **query-scoping** (MySQL has no equivalent to Postgres
  RLS), injecting the tenant filter/field on every operation.
- **MongoDB, row-level** — the Mongoose adapter's **query facade** (see the caveat above).
- **Database-per-tenant** (PostgreSQL) — physically separate databases, cache-routed per tenant.

## What's _not_ there yet

- **Prisma schema-per-tenant** is intentionally unsupported: Prisma resolves tables from the datasource,
  not the session `search_path`, so a per-schema `search_path` doesn't route its queries. Use
  database-per-tenant for hard isolation with Prisma.
- **TypeORM / Sequelize** ship row-level today; schema- and database-per-tenant for them are on the
  roadmap, not yet adversarially proven.
- **MySQL / MongoDB** beyond row-level would need mechanisms those databases don't offer the way Postgres
  does — so row-level is the supported model there by design.

## The CLI tells you too

You don't have to memorise this. `tenancy tenant check` reads each configured adapter's own capability
self-report and warns you — to your face — about any combination that isn't tested-supported:

```ansi
WARN adapter:prisma: schemaPerTenant is reported as "unsupported", not
     tested-supported — this combination is not in the verified matrix;
     use at your own risk
```
