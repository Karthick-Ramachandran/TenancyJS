# Module Test Plan: Cli

## Unit Tests

- Detection, plans, templates, static Doctor rules, redaction, schemas, and effort estimates.

## Integration Tests

- Fixture dry-run/apply/idempotency, binary exits/JSON, local Node pass/fail, and package consumer.

## Security Tests

- Traversal/absolute/symlink/conflict/race/rollback, `.env` canary, secret URLs, child environment,
  timeout/output bounds, and no shell/remote execution.
