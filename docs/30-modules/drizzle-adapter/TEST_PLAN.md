# Test Plan

Unit tests cover configuration, classification, protected operations, failure states, and sanitized
errors. Real PostgreSQL and MySQL tests follow F-013's colliding-ID row/schema/database plan. Package
release requires build, type declarations, packed-consumer import, and the repository gate.

